<Window x:Class="Login.Cadastro"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Cadastro" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <TextBlock
                Text="Cadastro de Usuário"
                FontSize="32"
                TextAlignment="Center"
                Margin="0 0 0 20" />

            <!-- TextBlock/ TextBox / Button -->
            <TextBlock Text="Nome de usuário" FontSize="20" />
            <TextBox FontSize="16" />

            <TextBlock Text="Senha do usuário" FontSize="20" />
            <TextBox FontSize="16" Margin="0 0 0 20" />

            <Button Content="Cadastrar" FontSize="20" Click="BotaoCadastrar" />
        </StackPanel>
    </Grid>
</Window>